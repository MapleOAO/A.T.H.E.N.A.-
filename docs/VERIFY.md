# 验证记录

日期：2026-09-18，Node.js 24.19.0，版本 v0.2.0。

## 自动化通过

- npm run validate：172 实体 / 81 关系 / 10 来源，48 关系 pending。
- npm test：36 项，无失败、跳过。
- npm run build 与 node --check web/app.mjs 通过。
- 原有数据约束、导入、查询、翻译和 HTTP 安全边界测试保留。
- 新测试覆盖：漏掉目录或关联即失败、Liao/Echo 双输入去重保留出处、全部英雄有批准译名、未确认异拼不合并。
- 布局测试覆盖全图、一跳、空图：每个实体恰有坐标、坐标在界内、节点间距、确定性。

## 已执行的线上浏览器验收

2026-09-18 访问 https://mapleoao.github.io/A.T.H.E.N.A.-/，真实浏览器成功加载 v0.2，完成桌面截图检查。

- 默认安娜一跳显示9实体/8关系，完整目录172项。
- 搜索回声显示19实体/18关系；选择其详情并打开廖博士关系，两个 Atlas 输入定位均保留。
- 术语搜索“桥元”返回正确官方名称；选择待审后该行消失。
- 无结果搜索显示空状态；重置后按已核验筛选显示33条关系。
- 缩放到120%、复位、Enter选择安娜母女关系，正确显示证据与 /connections/2。
- 五个导航可用，来源目录10项；收录进度计数与本地数据一致。
- 站点域名下无控制台错误；只看到浏览器扩展 metadata 错误。

以下为完整回归清单；其中窄屏、拖拽、离线文件实际打开及JSON下载尚未进行浏览器验收。离线文件只完成模块语法、内嵌数据与无启动网络请求检查。

1. 默认安娜一跳；目录仍可搜索全部实体。输入“回声”能看到其邻接网络。
2. 清空搜索、关闭一跳后为 172 实体 / 81 关系；仅已核验为 33 条关系。
3. 点击安娜→法老之鹰，证据和 /connections/2 可见。
4. 回声→米娜·廖：创造方向为廖→回声，保留 /connections/10 与 /connections/37。
5. 术语页搜索“桥元”，已核对；IJC 等条目仍待审。
6. 收录进度为英雄53/53、其他角色76/76、组织41/41、关联57/57；译名61/172、关系33/81。
7. 五个导航视图、搜索无结果、待核验返回图谱、hash 定位、键盘选择可用。
8. 缩放、拖拽、复位和 390px 窄屏无整页横向溢出。
9. Pages 子目录全部相对资源加载成功，控制台无错误。
10. preview.html 直接打开，无需服务器；首屏、筛选、详情、术语页正常。

## v0.3 自动化验证

- 172实体、105关系（58已核验 / 47待核验）、13来源。
- 37项测试、校验、构建、app.mjs语法通过；新增图像来源与不可信域名拒绝检查。
- 已在线实测 v0.3.0 / 172实体 / 58已核验 / 47待核验；截图确认图谱、列表和详情头像正常，当前可见头像 naturalWidth 大于0。
- 查询廖博士并选择卡西迪，正确显示“卡西迪 → 米娜·廖 / 保护”、国服回声章节及 /connections/9；廖博士缺图说明明确显示“名称占位图”。
- 站点域名未发现控制台错误。本次没有逐张验证全部53张头像在每种浏览器下的加载结果。

## v0.4 自动化验证

- 172实体、302关系（260已核验 / 42待核验）、70来源；117已核对译名。
- 39项测试全部通过，validate / build / node --check web/app.mjs通过。
- 新增场景身份说明缺失、原图局部坐标越界、黑爪之治与历史归属分开的回归检查。
- 53英雄头像、15官方剧情配图、2官方组织标志；名称占位102项不算作官方图像。
- 《蜕变之痛》PDF第3–5、11页渲染核对了MEKA队员称呼与画面；《破咒之镰》第4页渲染核对敏郎。
- 线上验收：知识库 v0.4.0、172实体、260已核验、42待核验；图像目录组织+已有官方图像返回8项。
- 浏览器全页截图确认守望先锋/黑爪标志，以及6个组织剧情配图正常；点击MEKA小队进入7条关系的局部网络，成员裁切图像正常。
- 点击明队长关系显示《蜕变之痛》PDF第6页；敏郎详情显示14条关系、配图身份说明及译名来源。
- 按站点域名筛选的控制台错误为空；浏览器扩展metadata错误与本项目无关。未逐张验收全部70项图像或重做移动端检查。
- 发现并修复新版HTML混用旧缓存问题：CSS、应用模块、依赖模块和知识库JSON使用一致内容指纹 ded7f03645573050；重新部署后版本与统计一致。

## v0.5 本地验证

- 172实体、315关系（275已核验/40待核验）、77来源、79官方图像。
- 42项测试通过；新增局部图像路径/页码/边界/源PDF哈希拒绝检查、JPEG字节哈希、HTTP图像内容类型。
- validate、build、app模块语法通过；preview内嵌7张JPEG与dist文件逐字节一致。
- 逐张查看7项实际局部，原作页码和身份说明在visual及研究审计中；新增2项外链剧情配图。
- 线上实际浏览器确认v0.5.0、275已核验、40待核验、79官方图像；待补图筛选返回93实体。
- 埃米莉截图中图谱/详情漫画头像正常；埃米莉和Palak详情JPEG的naturalWidth大于0，页码及原PDF链接可见，Palak仍显示“暂译·待确认”。
- 廖→奥罗拉“研究对象”详情显示Echo PDF第1–3页，并保留Atlas /connections/17作为发现来源。
- 按站点域名筛选控制台错误为空。未重做移动端和全部79张图片逐项浏览器检查。
- 单文件preview实测启动成功，埃米莉详情两处图像均从data:image/jpeg内嵌数据加载；没有将此检查宣称为断网浏览器测试。
