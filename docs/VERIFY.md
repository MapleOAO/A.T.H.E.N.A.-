# 验证记录

日期：2026-09-18，Node.js 24.19.0，版本 v0.2.0。

## 自动化通过

- npm run validate：172 实体 / 81 关系 / 10 来源，48 关系 pending。
- npm test：36 项，无失败、跳过。
- npm run build 与 node --check web/app.mjs 通过。
- 原有数据约束、导入、查询、翻译和 HTTP 安全边界测试保留。
- 新测试覆盖：漏掉目录或关联即失败、Liao/Echo 双输入去重保留出处、全部英雄有批准译名、未确认异拼不合并。
- 布局测试覆盖全图、一跳、空图：每个实体恰有坐标、坐标在界内、节点间距、确定性。

## 待进行的真实浏览器验收

本地服务未能通过云浏览器网络边界；尚未进行实际视觉验收。不要将自动化结果当作浏览器截图验证。

1. 默认安娜一跳；目录仍可搜索全部实体。输入“回声”能看到其邻接网络。
2. 清空搜索、关闭一跳后为 172 实体 / 81 关系；仅已核验为 33 条关系。
3. 点击安娜→法老之鹰，证据和 /connections/2 可见。
4. 回声→米娜·廖：创造方向为廖→回声，保留 /connections/10 与 /connections/37。
5. 术语页搜索“桥元”，已核对；IJC 等条目仍待审。
6. 收录进度为英雄53/53、其他角色76/76、组织41/41、关联57/57；译名61/172、关系33/81。
7. 五个导航视图、搜索无结果、待核验返回图谱、hash 定位、键盘选择可用。
8. 缩放、拖拽、复位和 390px 窄屏无整页横向溢出。
9. Pages 子目录全部相对资源加载成功，控制台无错误。
10. preview.html 直接打开，无需服务器；首屏、筛选、详情、术语页正常。
