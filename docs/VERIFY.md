# 验证记录

日期：2026-09-18。运行时 Node.js 24.19.0。

## 已执行

- `npm run validate`：通过，18 实体 / 21 关系 / 7 来源。
- `npm test`：32 项测试通过，无失败或跳过。
- `npm run build`：生成独立静态文件，无远程资产依赖。
- `node --check web/app.mjs`：通过。
- HTTP：健康检查、前端数据结构、页面／脚本／样式 MIME、CSP、实体接口、未知实体 404、禁止 POST、HEAD 无 body、私有路径与编码路径穿越防护。
- 数据：悬空端点、重复 ID、重复关系、无效谓词端点、错误审核／时期、未读来源、仅社区证据、术语版本／ID、候选端点错配、危险 URL。
- 导入：v5 格式、固定 SHA、名字白名单、类型错配、去重、不接受上游文字或布局作为事实。
- 查询／中文化：中英文一致、别名、状态、时期、实体类型、一跳、无结果、待确认词阻止批准。

## 未通过环境边界

云浏览器无法访问开发服务，错误为 `net::ERR_BLOCKED_BY_CLIENT`。不能声称实际浏览器功能或视觉布局已通过。服务能返回 HTML 不足以证明前端行为正常。

## 下一次浏览器验收清单

1. 桌面 1440×900：主导航、图谱、筛选与详情可见，无 JS 错误或加载占位。
2. 搜索“安娜”与“Ana”获得同一邻接网络；搜索不存在的名字显示空结果，重置恢复 18/21。
3. 仅已核验显示 17 条边；仅组织不出现人物端点；召回时期不过滤成当前赛季断言。
4. 点击安娜→法老之鹰，详情显示母女方向、官方 RECALL 证据、Atlas /connections/2、术语版本。
5. 点击待核验记录，不变成已核验；Search & Rescue 显示暂译；相关中文化 CLI 返回 pending。
6. 一跳聚焦、放大、缩小、平移、复位；实体和边用 Tab / Enter / Space 可选择。
7. 四个导航视图均有真实内容；待核验卡片可回到图谱；哈希链接能定位实体／关系。
8. 390×844 窄屏：筛选在上方、详情在图下方、表格可横向滚动、无整页意外横溢。
9. 200% 缩放及减少动画偏好；所有正文可读。
10. 静态 `dist/` 在根路径与子目录均能加载；断网资源错误能显示重试，不永久空白。
